import { TStringTranslatePipePipe } from './tstring-translate.pipe';

describe('TstringTranslatePipe', () => {
  it('create an instance', () => {
    const pipe = new TStringTranslatePipePipe();
    expect(pipe).toBeTruthy();
  });
});
